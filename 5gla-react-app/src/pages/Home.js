//import { Container, Typography } from '@mui/material';
import { Link, useNavigate } from 'react-router-dom';

function HomePage(){
    const navigate = useNavigate();

    return (
        <>
        WELCOME TO 5GLA-REACT-VISUALIZATION
        </>
    )
}

export default HomePage;